# CS_Camera
The objective of the compressed camera sensor project is to design and build a functional prototype camera with a single pixel sensor to capture visible light. After capturing the raw data, we use a laptop to reconstruct the image.
